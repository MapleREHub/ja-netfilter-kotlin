; ============================================================================
; - env.conf - 环境变量过滤规则
; ----------------------------------------------------------------------------
; 用于 env 插件，hook java.lang.ProcessEnvironment 方法。
;
; 格式：
;   [ENV]
;   EQUAL,VARIABLE_NAME
;
; 注意：
;   原版的 env.conf 是空文件，表示不主动替换环境变量。
;   此处保留示例规则用于演示。
; ============================================================================

[ENV]
; put env replacement rules here